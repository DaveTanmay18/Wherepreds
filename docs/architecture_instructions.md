I want to create a football predictor application where the users can create their own leagues for different leagues in world football. So for that, i would want to first of all manage the whole of data for top 5 leagues including the players details, their basic stats, football clubs, club stats along with league stats.

Also create a neat design for the predictor application so that it can also be used efficiently and easily in mobile view. Make the theme look simple and easy to read instead of cluttering things which might ruin the ux of the web application.

The main aim of this predictor application is that every user can create their own league with their custom rules, for example, different users can have different point systems and the methods of playing the prediction like different point system, bonus points, leeway to manage predictions on timers (basically user can set it according to their mind). So these are some of the points that can be used to craate a league where they can have their own competition on their own rules.

I also want to create the whole database schema and give me the full architecture of the schema in the file.

I would like to use PERNstack for the whole project along with neon db and prisma.